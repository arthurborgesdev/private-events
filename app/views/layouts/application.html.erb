<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar"> 
      <% unless session[:current_user_id] %>
        <p>Please Enter as a User To create posts</p>
        <%= link_to 'Create User', new_user_path %>
      <% else %>
        <%= link_to 'New Event', new_event_path %>
        <p><%= User.find(session[:current_user_id]).name %> is logged in</p> 
        <%= link_to 'Log out', reset_path, method: "post" %>
        <td><%= link_to 'Invite to event', invite_path %></td>
      <% end %>
      <%= link_to 'Events Page', events_path, method: "get" %>
      <%= link_to 'Authors Page', users_path, method: "get" %>
    </nav>
    <%= yield %>
  </body>
</html>
